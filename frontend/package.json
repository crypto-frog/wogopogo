{
  "name": "wogopogo",
  "private": true,
  "version": "1.3.0",
  "description": "Account-free community job board for the Okanagan Valley",
  "license": "MIT",
  "engines": {
    "node": ">=22.13.0"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:mock": "vite --mode mock",
    "build": "vite build && node ../scripts/generate-static-pages.mjs",
    "package:bluehost": "npm run build && node ../scripts/sync-deploy.mjs",
    "preview": "vite preview",
    "test": "node --test"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-router-dom": "7.18.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.1.0",
    "vite": "8.2.2"
  }
}
